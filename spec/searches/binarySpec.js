/** For practical  purposes, the functions used and tested by this spec should return answers as tuples.
 * The purpose of this is to measure the performance of the search.
 * There is a chance that the linear search could return an object more quickly than the binary search.
 * However, we are looking for the average! Our data-set is extremely small as well, compared to what could
 * be reasonably expected for a binary search. 
 */

describe('binary.js', function() {
  it('should find an object in less steps than a linear search', function() {

  });
});






var linearSearch = function(array, expected) {
  var result;
  var count = 0;
  return [result, count];
};